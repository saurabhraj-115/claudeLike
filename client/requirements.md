## Packages
react-markdown | Rendering markdown in chat messages
lucide-react | Beautiful icons for the interface
date-fns | Formatting dates in history
framer-motion | Smooth animations for messages and sidebar
react-textarea-autosize | Auto-expanding chat input

## Notes
API Key for Anthropic is stored in localStorage as 'anthropic_api_key'
Chat streaming is simulated via optimistic updates for MVP, but ready for real streaming if backend supports it.
